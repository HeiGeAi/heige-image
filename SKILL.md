---
name: heige-image
description: |
  需求驱动的中文设计生图产品，内置设计总监 agent 守审美。用户指定了画风、配色、场景就照做；没指定就根据配套文件（要配图的文章、品牌规范、参考图）自动推断最合适的设计方案，每个决策带理由。按图类型路由引擎：文字要逐字精确的版式图走免费 HTML 渲染，画面叙事的插画概念图走 API 引擎。
  触发条件（任意满足即调用）：
  (1) 用户要求生成图片、画图、生图、做图、出图、海报、封面图、配图、设计图、概念图
  (2) 用户要求编辑图片、修改图片、图生图（支持多图输入：图1基底+图2人脸/风格参考）
  (3) 用户上传图片并要求修改
  (4) 用户说 /heige-image
  (5) 用户说批量生成图片
  (6) 用户点名某种画风、配色或设计风格
---

# heige-image

## 这是什么

**需求驱动的中文设计生图产品，内置一位设计总监。** 别人的生图工具是「给个提示词出张图」，这个是「先想清楚要什么图，让设计总监定方案，再选对引擎出图」。没有写死的画风预设，风格跟着这一次的需求走。

三条铁律，记死：

- **用户需求最大**。用户明确指定的画风、配色、场景、比例，一律照办，设计总监只补缺口，无权改判。
- **没指定就推断，别乱选也别反问**。用户没说的维度，设计总监从配套文件（要配图的文章、品牌规范、参考图、同项目已有图）里提取主题、情绪、既有色彩，推断出最合适的设计方案，每个决策带一句理由。
- **审美有人守门**。设计总监在两道闸口出场：出图前产出《设计方案》，出图后按方案逐项验收。方案先行，规格可复现，改图不漂移。

三条引擎按图类型分：要逐字精确的版式图（封面/信息图/卡片）走无 key 的 HTML 渲染引擎，浏览器截图、免费、文字不崩；要画面叙事的插画概念图走 API 引擎，编译可复现的规格再出图；改已有的图走图生图引擎。

## 何时触发

所有生图 / 图生图 / 批量出图需求默认进这里，不分语境：

- **用户带明确要求**（点名画风、给了配色、描述了场景）：照用户说的做，设计总监补齐没说的维度。
- **用户只给大白话**（「给这篇文章配个封面」「画个 AI 干活的图」）：走完整五步，设计总监根据配套文件定方案。

**不要触发的情况**：要小红书纯运营发布（去 xiaohongshu）、要 PPT/幻灯片（去 baoyu-slide-deck）。

---

## 工作流五步（核心，照着走）

**别跳步直接出图。** 五步全程在岗，没 key 也能跑前四步，至少交付一份设计方案加规格文件。

### 第一步：收集配套文件

生图需求几乎从不孤立出现，先把手边的上下文吸进来：

| 配套文件 | 提取什么 |
|----------|----------|
| 要配图的文章 / 文案 | 主题一句话、情绪调子、受众、文中的具体意象（优先画文中真实出现的场景，别另编隐喻） |
| 品牌规范 / 设计规范 | 品牌色 hex 直接进色板、字体气质、禁用项 |
| 用户给的参考图 | 画风、构图、材质光感，按图直接反推词汇 |
| 目标目录已有图片 | 同系列的既定风格，新图跟着走保持成套 |
| 项目 README / 产品介绍 | 产品调性（工具向冷静、社区向活泼这类） |

一个都没有也能走：跳到第二步，风格决策落到风格库默认，方案里注明「无配套文件，按载体默认」。

### 第二步：需求翻译（大白话 → 7 维 brief）

把需求翻成结构化 brief，缺的维度别反问，交给第三步的设计总监补：

| 维度 | 问什么 | 填法 |
|------|--------|------|
| **载体** | 这图用在哪 | 公众号封面 / 信息图 / 小红书组图 / 文章章节图 / 金句卡，载体决定后面一切默认 |
| **受众** | 给谁看 | 从业者 / 普通读者 / 老板，影响隐喻密度和画面复杂度 |
| **主题** | 这图在说什么一件事 | 一句话说穿，比如「AI 嘴上认错身后按删除键」 |
| **情绪** | 什么调子 | 治愈 / 专业 / 锐利 / 轻松，从配套文件的行文调子里读 |
| **文字层级** | 图上有没有字、几级 | none（纯视觉）/ title-only（一句大标题）/ text-rich（多级），决定走哪条引擎 |
| **比例** | 什么形状 | 横 / 竖 / 方三个桶 |
| **风格线索** | 用户点名了什么 | 画风、配色 hex、参考图、「跟上次那张一样」，原话记下；没点名就写「待设计总监定」 |

成功标准：**7 维全有值（风格线索允许待定），主题能一句话说穿。**

### 第三步：设计总监出《设计方案》（内置设计 agent）

这是这个产品的命根子。把 brief 和配套文件摘要交给设计总监，产出一份《设计方案》再往下走。设计总监的完整定义在 `references/design-director.md`，两种跑法：

- **有 Agent 工具的环境**（Claude Code 等）：派一个子 agent，让它读 `references/design-director.md` 后按其中的角色和流程产出《设计方案》。
- **没有 Agent 工具的环境**：主对话自己切进设计总监角色，照同一份文件走完流程再切回来。产出物和质量标准完全一样。

设计方案固定五件，每件都内联标注来源等级（用户指定 / 配套文件推断 / 库默认）和一句理由：

1. **画风**：从 `references/style-library.md` 选一个或组合两个，用户点名库外风格也照做
2. **色板**：3 到 5 个 hex，主色 / 强调色 / 底色分明
3. **场景与构图**：画什么、主体是谁、视线怎么引导，优先用配套文件里的真实意象
4. **文字与排版处理**：图上的字怎么摆，或明确「纯视觉零文字」
5. **风险提示**：用户要求里有翻车风险就写清并给替代做法，没有就写「无」

**决策顺序铁律：用户指定 > 配套文件推断 > 风格库默认。** 高等级来源永远压过低等级，设计总监的个人审美只能填空，禁止推翻用户明示的要求。

### 第四步：查决策矩阵定引擎，编译规格

拿着设计方案查 `references/design-decision-matrix.md`，认载体、定引擎、抄构图默认，落成规格文件 `prompts/NN-主题.md`（格式见下方规格文件机制）。

引擎分流看「文字层级」这一维：

- `title-only` 或 `text-rich`（图上有要逐字精确的中文标题、术语、数据）：**走无 key 版式渲染引擎** `scripts/render.py`。HTML/CSS 把字焊死在版面上，截出来一个像素不差，免费。设计方案的色板写进模板的 custom 强调色块。
- `none`（纯视觉）或治愈插画、概念画面：**走 API 插画引擎** `scripts/gen.py`。把设计方案编译成结构化英文 prompt，风格词汇从方案里取，色板 hex 写死进 prompt，别让模型自己挑色。
- 改已有的图：**走图生图引擎** `scripts/edit.py`。

插画 prompt 按 8 段组织（顺序即结构）：风格词汇（从设计方案取）→ 主体 → 场景与构图 → 氛围道具 → 色板 hex → 光感材质 → 比例意图 → 控字句加负向收尾。负向收尾至少带：`no watermark, no signature, no clutter`，再按画风补充该风格的禁忌项（风格库每个词条列了）。

### 第五步：出图与验收

跑脚本出图，然后设计总监二次出场，按两份清单验收：

1. **方案符合性**：成品和《设计方案》逐项对：色板对不对、画风对不对、场景对不对、文字层级有没有越界。
2. **反 AI 体检**：过 `references/anti-patterns.md` 的成品体检段（手 / 字 / 对称物崩没崩、有没有通用 AI 油光）。

任意一条中招就回炉，**回炉最多 1 轮**。1 轮还不行就降级处理（换主体重写 prompt，或换引擎），别无限重试烧钱。

---

## 任务路由（三条引擎，按图类型选）

| 图类型 | 特征 | 走哪条引擎 | 脚本 | key |
|--------|------|-----------|------|-----|
| **版式图（封面/信息图/卡片，文字要逐字精确）** | 文字是主角，标题、术语、副标题一个字都不能错 | **无 key 版式渲染引擎**：选 HTML 模板→填真实数据→Playwright 截图 | `scripts/render.py` | 不要 key，免费 |
| **插画 / 概念图 / 示意图** | 文字少（≤30%）或纯视觉，靠画面叙事 | **API 插画引擎**：编译英文 prompt 送 gpt-image 类模型 | `scripts/gen.py` | 要 key（自己配 base_url + key，接任意 OpenAI 兼容 API） |
| **图生图 / 改图** | 有原图，要换背景、改元素、多图合成 | **图生图引擎**：打 `/images/edits` 端点 | `scripts/edit.py` | 要 key，同 gen.py |

**为什么这么分**：扩散模型本质画像素，排版不是它的活，文字稍长就拼错、字距乱、中文尤其崩。要精确版式就别交给 API，交给浏览器渲染引擎。要画面叙事的质感就别用 HTML 硬拼，交给扩散模型。两边各干各擅长的。

**拿不准的中间情况**：想要一张「有大标题 + 插画背景」的封面，先拆。标题精确性优先就走版式引擎（干净留白配一个强调色已经够有调性）。背景画面叙事优先、标题能压进一行短中文（≤14 字），就走 API 引擎并在 prompt 里带控字句。别想着一条引擎全包。

---

## 规格文件机制（先落 prompts/NN.md 再出图）

**规格即真相。每张走 API 引擎的图先落规格，再出图。改图只动规格，不绕过规格直接改 prompt。** 版式图的真相落在填好的 HTML 文件里，规格机制不强制它。

每张图在 `prompts/` 下落一个 `NN-主题.md`（NN 是两位序号），内容固定四块：

```
# 01-主题名

## Brief（7 维）
载体：文章章节图
受众：AI 圈从业者 + 普通读者
主题：小机器人帮你把重复活儿交给脚本
情绪：治愈、轻松
文字层级：none
比例：16:9
风格线索：用户没点名，待设计总监定

## 设计方案（设计总监产出，每项带来源等级）
画风：软萌 3D（库默认，理由：教程类文章要降低门槛感）
色板：#F5A8B8 / #FBF3EC / #F6ECE4（文件推断，理由：文章已有配图是暖色系）
场景与构图：机器人对着浮空积木堆比划，道具悬浮环绕（文件推断，取自文中「搭积木」比喻）
文字与排版：纯视觉零文字（载体默认）
风险提示：无

## 最终 Prompt
\`\`\`
（送进 API 的那段完整英文 prompt，脚本就读这一块）
\`\`\`

## Invariant 清单（改图时必须逐条锁死）
- 色板：#F5A8B8 / #FBF3EC / #F6ECE4
- 主体：圆润小机器人，动作是对着积木堆比划
- 材质：哑光软陶、圆角无棱、柔光软影
```

**脚本只读「最终 Prompt」小标题下的第一个围栏代码块。** 这个小标题和代码块是硬约定，少了脚本报错。比例可以写进规格（`比例: 16:9`），脚本会自动捞。上面示例块里的反引号带了转义符（外层代码块所迫），照抄格式时以仓库里的 `prompts/01-示例-章节图-配套文件推断.md` 为准，那份能直接跑通 `gen.py --spec`。

**改图怎么改**：只动 invariant 里的单一变量（比如只换主色），其余 invariant 逐条复述进新 prompt，别整段重写，否则就漂了。改完落一个新序号的规格文件，老的留着对照。

---

## 怎么调 scripts/render.py（无 key 版式渲染引擎）

版式图（封面/信息图/卡片，文字要逐字精确）走这条。**零 API、免费、文字一个像素不差。** 机制是种子 HTML 加 Playwright 截图，版面气质是简约编辑风（留白当主角、克制强调色、发丝线网格），具体配色由设计方案定。

三步走：**选模板→填真实数据→截图**。

**第一步：选模板**。`templates/` 下按载体挑：

| 模板 | 载体 | 尺寸（焊死在 CSS） |
|------|------|------|
| `templates/cover-clean.html` | 公众号 / 文章 / 小红书封面（单一焦点 + 大留白） | 默认竖板 1080×1440，可切 square 1080×1080、wide 2100×900 |
| `templates/infographic-clean.html` | 信息图 / 要点 / 对比 / 流程 / 数据清单（多级文字） | 默认竖板 1080×1440，四种数据形态切换 |

**第二步：填真实数据**。模板正文是一套示例数据，照顶部注释列的变量含义（`{{TITLE}}` 对应主标题这类），把示例文字逐处手动替换成真内容，不靠模板引擎，直接改 HTML。强调色切换：内置几组 `data-accent` 色板可直接点名，设计方案有自己的色板就填进 `[data-accent="custom"]` 那一块（模板里留好了空位）。标题口语、短、断行用 `<br>`，标题太长砍字别缩字号。

**第三步：截图**。模板里每张版式图包一个 `<section class="poster ...">`，render.py 按节点 bounding box 出图，节点多大就出多大像素，所见即所得。

```bash
# 截某个节点，指定输出（最常用）
python3 scripts/render.py templates/cover-clean.html --node "#cover" -o outimage/01-封面.png

# 不指定节点，抓页面里所有 section.poster 逐张出（信息图多卡片时）
python3 scripts/render.py templates/infographic-clean.html -o outimage/

# 默认 2x 高清，慢网或重渲染把 settle 调大
python3 scripts/render.py templates/cover-clean.html --node "#cover" --scale 2 --settle-ms 1200
```

不传 `-o` 默认落 `outimage/`，文件名按节点 id。`--full-page` 是兜底，模板没标 `.poster` 时强截整页。

**为什么文字不崩**：字是 HTML 文本，浏览器拿系统 PingFang SC 渲染，断网也不出豆腐块。版面全是纯 CSS，不依赖任何 API 和外部图片。

**简约的命门是克制**：留白当主角，整张不超 3 色，一个强调色点题，别堆装饰形状。照 `references/design-decision-matrix.md` 那条单轮 checklist 过一遍。

**首次用要装依赖**（脚本检测到没装会再提示一遍）：

```bash
pip install -r requirements.txt && playwright install chromium
```

**headless 已焊好的坑**：脚本默认带 `--use-angle=swiftshader`，否则 SVG / WebGL 滤镜在无头模式下会截成空白。截图前等 `document.fonts.ready` 再多停 800ms，避开截到 fallback 字体那一帧。这两个坑已经焊进 render.py，调用方不用管。

---

## 怎么调 scripts/gen.py（API 插画引擎）

插画/概念图/示意图走这条。设计方案定完、规格文件落好，由脚本出图。

**接口通用，自己配 base_url + key + model**。脚本走 OpenAI 兼容的图像生成接口，三项配置方式都一样，优先级从高到低：命令行 > 环境变量 > 配置文件 > 默认值。

| 配置项 | 命令行 | 环境变量 | 默认值 |
|--------|--------|----------|--------|
| API 基址 | `--base-url` | `HEIGE_IMAGE_BASE_URL` | `https://api.openai.com/v1` |
| API Key | `--api-key` | `HEIGE_IMAGE_API_KEY` | 无（必填一项） |
| 模型 | `--model` | `HEIGE_IMAGE_MODEL` | `gpt-image-2` |

配置文件放 `~/.heige-image/config.json`，内容：

```json
{"base_url": "https://api.openai.com/v1", "api_key": "sk-xxx", "model": "gpt-image-2"}
```

官方 OpenAI、Azure、各家中转都能接，填自己那把就行。

> 国内想直接出图、不想自己折腾通道，可以用 gptx.cc 这个渠道，base_url 填 `https://api.gptx.cc/v1` 加上自己的 key 即可，作为推荐选项之一，不写死也不强制。

**主推：规格驱动出图**

```bash
python3 scripts/gen.py --spec prompts/01-主题.md -ar 16:9
```

不传 `-o` 默认落到 `outimage/01-主题.png`，比例不传就读规格里写的，规格也没写就 1:1。

**临时直给提示词（不落规格，不推荐常用）**

```bash
python3 scripts/gen.py --prompt "完整英文 prompt" -ar 16:9 -o outimage/test.png
```

**并发批量（一组配图）**

```bash
python3 scripts/gen.py --batch tasks.json --workers 2
```

`tasks.json` 是数组，每项 `{"prompt": "...", "output": "outimage/xx.png", "aspect_ratio": "16:9"}`。批量张数超 `--max-n`（默认 6）会被成本护栏直接拦。

**比例只有三个真实桶**：横→1536×1024、竖→1024×1536、方→1024×1024。写 `21:9` 跟写 `16:9` 出来一样大，比例是构图意图，实际像素看桶。章节图 / 16:9 总结图都走横桶 `-ar 16:9`，小红书插画走竖桶 `-ar 3:4`。

---

## 怎么调 scripts/edit.py（图生图 / 图片编辑引擎）

上传本地图片 + 文字编辑描述，模型按描述改图。配置约定和 gen.py 完全一致（CLI > `HEIGE_IMAGE_*` 环境变量 > `~/.heige-image/config.json` > 默认值），打的是 `/images/edits` 端点。

**单图编辑**：

```bash
python3 scripts/edit.py \
  --input /path/to/original.jpg \
  --prompt "把背景换成古镇青石板路，人物姿态和服装不变" \
  -ar 3:4 -o outimage/edited.png --retry 3
```

**多图输入**（`--input` 可重复传入，图1=基底图，图2 及之后=人脸/风格参考图，prompt 里按「图1/图2」指代各图角色）：

```bash
python3 scripts/edit.py \
  --input /path/to/scene.jpg \
  --input /path/to/face.jpg \
  --prompt "以图1为场景基底，把图2的人物自然融入画面中央，保持图1的光线和构图" \
  -ar 16:9 -o outimage/merged.png --retry 3
```

**并发批量**（`input` 字段接受字符串或数组）：

```bash
python3 scripts/edit.py --batch tasks.json --workers 2
```

```json
[
  {"input": "/path/a.jpg", "prompt": "换成雪景", "output": "outimage/a-snow.png"},
  {"input": ["/path/scene.jpg", "/path/face.jpg"], "prompt": "以图1为基底融入图2人物", "aspect_ratio": "16:9", "output": "outimage/merged.png"}
]
```

细节：单图走 multipart 的 `image` 字段，多图走多个 `image[]` 字段；输入支持 jpg/jpeg/png/webp/gif，单张 < 10MB；输出只接受完整 PNG，下载带有界流式读取和重定向，比例三桶规则和 gen.py 相同。

---

## 引用四个 references 的时机

| 文件 | 什么时候读 |
|------|-----------|
| `references/design-director.md` | **第三步和第五步必读**。设计总监的角色定义、配套文件推断规则、《设计方案》格式、验收 checklist、降级跑法都在这。 |
| `references/style-library.md` | **设计总监选画风时必读**。每个风格词条的定位、prompt 词汇、配色倾向、适用场景、禁忌项都在这。用户点名画风也来这查词汇。 |
| `references/design-decision-matrix.md` | **第四步定引擎时必读**。四大载体的引擎分流、构图默认、单轮 checklist、三条全局铁律都在这。 |
| `references/anti-patterns.md` | **出图前过 prompt、出图后过成品时读**。反 AI 体检清单两段 + 能力圈声明。拿不准这张是不是 AI 味，翻这个。 |

---

## 成本护栏

**只管 API 引擎（`gen.py` / `edit.py`）。** 版式引擎（`render.py`）零 API、不烧额度，随便截，护栏不约束它。

API 引擎烧的是真额度，两道护栏卡死：

- **单次张数上限**：`gen.py --max-n` 默认 6，批量超了直接拦。要调高得明确知道在烧多少钱。一组配图压在 6 张内。`edit.py` 的批量没有硬拦，同样按 6 张的口径自我约束。
- **回炉最多 1 轮**：出图后过验收，中招就回炉重出，**最多回炉 1 轮**。1 轮还不行就降级处理（换主体重写 prompt，或换引擎），别无限重试烧钱。

---

## 能力边界（诚实）

**现在能做（三个脚本都已落地）**：

- **版式图（无 key 引擎，`render.py`）**：公众号封面、信息图、卡片这类文字要逐字精确的图。HTML/CSS 焊死版面，Playwright 截图，免费、不烧额度、中文一个像素不差。配色由设计方案定，模板留了 custom 色板位。
- **插画图（API 引擎，`gen.py`）**：插画、概念图、示意图。设计总监五步全程在岗，规格文件可复现可改单变量，画风按需求走，不锁任何一种。
- **图生图（API 引擎，`edit.py`）**：单图编辑（换背景、改元素）和多图合成（图1基底 + 图2人脸/风格参考），批量并发同 gen.py。

**各自需要什么**：

- 版式引擎要 Playwright + chromium（`pip install -r requirements.txt && playwright install chromium`），装一次到处用，不要 API key。
- API 引擎要一把能用的 key（自己配 base_url + key，接任意 OpenAI 兼容 API，国内可选 gptx.cc 这个渠道）。没 key 前四步照跑，能交付设计方案和规格文件，就是出不了图。

**还是不做**：

- **API 引擎的图里文字别指望写对**，压在 30% 以内且默认可能拼错。要可读正确的文字走版式引擎那条。
- **API 引擎尺寸只有三种**，别信脚本里那十种比例，全映射到方/横/竖三个桶。要真正超宽幅/精确画幅 API 做不到，版式引擎可以在 CSS 里焊任意尺寸。
- **复杂插画质感别用版式引擎硬拼**，HTML 截图只到「简约版式 + 精确文字」这层，画面叙事交给 API 引擎。
- **设计总监不越权**。用户明示的需求永远优先，设计总监只在用户没说的地方补专业判断，禁止拿「更好看」推翻用户要求。

**一句话定位**：要逐字精确的版式图走无 key 的 `render.py`，免费且文字不崩；要画面叙事的插画概念图走要 key 的 `gen.py`。用户说了算，没说的设计总监按配套文件推断，每个决策带理由，规格可复现。
