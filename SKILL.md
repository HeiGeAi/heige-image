---
name: heige-image
description: |
  黑哥AI 的中文设计生图产品，本机默认生图渠道。按图类型路由（不按 key），把大白话需求翻译成结构化规格再出图，插画走粘土 3D 卡通签名、版式走 guizang 简约设计，另有通用引擎模式承接非黑哥语境的出图需求。
  触发条件（任意满足即调用）：
  (1) 用户要求生成图片、画图、生图、做图、出图、海报、封面图、配图、设计图、概念图
  (2) 用户要求编辑图片、修改图片、图生图（支持多图输入：图1基底+图2人脸/风格参考）
  (3) 用户上传图片并要求修改
  (4) 用户说 /heige-image
  (5) 用户说批量生成图片
  (6) 点名粘土风、治愈风、黑哥画风、guizang 版式
---

# heige-image

## 这是什么

**按图类型路由的中文设计生图产品。** 别人的生图工具是「给个提示词出张图」，这个是「先想清楚要什么图，再选对引擎，最后出图」。中间多了一颗设计脑，出来的图认得出是黑哥的，改图不漂移。

两条引擎按图类型分：要逐字精确的版式图（封面/信息图/卡片）走无 key 的 HTML 渲染引擎，浏览器截图、免费、文字不崩；要画面叙事的插画概念图走 API 引擎，编译可复现的规格再出图。

护城河就三样，记死：

- **黑哥私有语料**：两套定死的视觉签名，都在暖色治愈家族里。插画走粘土 3D 卡通（暖粉加奶油白 + 手工粘土哑光材质 + 粉白圆润小机器人吉祥物），版式走 guizang 简约设计（暖米白底 + 单一暖粉强调色 + 大量留白 + 发丝线网格）。别人调不出这个味。
- **可识别视觉签名**：盖掉标题单看图，认得出是黑哥这套，不是任何扩散模型随手吐的均值图。两套靠同一个暖色域绑在一起，并排看一眼能认出是一家。
- **规格文件可复现**：每张图先落 `prompts/NN-主题.md` 规格，记下 brief、设计决策、最终 prompt、invariant 清单。改图只动单变量，其余逐条复述，图不跑偏。

**账号红线先说清**：这是黑哥AI 账号视角，插画用粘土 3D 卡通、版式用 guizang 简约，两套都是黑哥的。别串其他账号的画风（锋利吐槽、写实冷峻科技风那些）。废弃旧版「波普拼贴讽刺」签名，那个选偏了。

## 何时触发

**本 skill 是本机默认生图渠道**，所有生图 / 图生图 / 批量出图需求默认进这里。分两种模式：

- **黑哥物料**（公众号配图、封面、章节图、治愈插画、概念图、吉祥物图，或点名黑哥画风/粘土风/guizang）：走完整设计脑三步 + 视觉签名，这是产品的命根子。
- **通用出图**（其他账号物料、客户项目、临时需求，语境明显不是黑哥AI 内容）：跳过视觉签名，直接调 `gen.py` / `edit.py` 引擎出图，别把粘土风套到别人账号上（账号红线）。

**不要触发的情况**：要小红书纯运营发布（去 xiaohongshu）、要 PPT/幻灯片（去 baoyu-slide-deck）。

---

## 设计脑三步工作流（核心，照着走）

这是这个产品的命根子。**别跳步直接出图。** 三步全程在岗，没 key 也能跑前面，至少交付一份规格文件。

### 第一步：需求翻译（大白话 → 6 维 brief）

用户给的几乎都是大白话，像「给我这篇文章配个封面」「画个 AI 谄媚的图」。先把它翻成结构化的 6 维 brief，缺的维度别反问，进第二步靠决策矩阵填默认。

六个维度，逐个落字：

| 维度 | 问什么 | 填法 |
|------|--------|------|
| **载体** | 这图用在哪 | 公众号封面 / 信息图 / 小红书组图 / 文章章节图 / 金句卡，认准载体决定后面一切默认 |
| **受众** | 给谁看 | AI 圈从业者 / 普通读者 / 老板，影响隐喻的圈内梗密度 |
| **主题** | 这图在说什么一件事 | 一句话说穿，比如「AI 嘴上认错身后按删除键」 |
| **情绪** | 什么调子 | 治愈 / 轻松 / 亲和 / 鼓励，黑哥画风默认带治愈温暖 |
| **文字层级** | 图上有没有字、几级 | none（纯视觉）/ title-only（一句大标题）/ text-rich（多级），见下方任务路由 |
| **比例** | 什么形状 | 横（封面/章节图）/ 竖（信息图/小红书）/ 方（金句卡），实际只有三个桶 |

翻译完，brief 写进规格文件顶部。这一步的成功标准：**6 维全有值，主题能一句话说穿。**

### 第二步：设计决策（查矩阵填有理由的默认）

用户没说的维度，**别留空、别反问**，查 `references/design-decision-matrix.md` 填有理由的默认值。

流程：

1. **认载体**：落到决策矩阵对应行（公众号封面 / 信息图 / 小红书组图 / 文章配图）。
2. **抄默认**：把那一行的六个默认（比例 / 风格 / 配色 / 文字层级 / 构图 / 坑）抄进规格文件的「设计决策」段。
3. **填偏离理由**：用户明确指定的维度覆盖默认，并补一句「这张为什么偏离默认」。没偏离就照抄。
4. **单轮自检**：照决策矩阵那张单轮 checklist 过一遍，过不了就地修，**修完不回头，不递归死循环**。

成功标准：**六个默认全有值且都带理由，单轮 checklist 全过。** 卡在某条来回改超过一轮，说明 brief 没定清，回第一步而不是在这死磕。

### 第三步：编译 prompt（按 OpenAI 官方 8 要点编结构化规格）

把 brief + 设计决策编译成一段送进 API 的结构化英文 prompt。骨架直接套 `references/heige-visual-signature.md` 第八节的「统一前缀 + 主体场景 + 统一光感材质收尾 + 负向收尾 + 控字句」。

按 OpenAI 官方 8 要点组织（顺序即结构）：

1. **风格前缀**：粘土 3D 卡通那一串（写死，每张都带）：`Claymation, clay 3D render, cute kawaii handmade plasticine style, matte clay texture, soft sculpted rounded surfaces, chubby inflated volume.`
2. **主体**：画面主角是什么，优先让粉白小机器人吉祥物出场（奶油白圆身子、暖粉耳机、胸口爱心、深棕黑屏幕脸），再补它在做什么动作
3. **场景与构图**：居中或黄金分割点放主角，道具像气泡悬浮环绕，留足空白，干净米白背景
4. **氛围道具**：粘土质感的小道具（积木 / 齿轮 / 文件卡 / 气泡 / 爱心 / 勾选标记），全部圆角奶色带柔影
5. **配色**：写死暖粉 `#F5A8B8` + 奶油白 `#FBF3EC` + 米白底 `#F6ECE4`，低饱和暖色治愈
6. **光感材质**：柔光无硬影 + 暖调全局照明 + 柔糊接触阴影 + 微弱次表面散射，哑光软陶质感，禁塑料反光、禁金属
7. **比例意图**：写构图意图（`cinematic ultra-wide banner composition`），真实桶在脚本层定
8. **控字句**：纯视觉隐喻图禁可读文字；16:9 总结图只留一行暖粉立体粘土中文字（≤14 字）。收尾固定带负向：`no high saturation, no neon, no cyberpunk, no harsh shadows, no glossy reflection, no metallic, no realistic photo, no dark background, no clutter.`

编完先落规格文件，再出图。成功标准：**这段 prompt 换个主题套不上去（够具体），invariant 清单写得出哪些必须锁死。**

---

## 任务路由（两条引擎，按图类型选）

按图类型分两条引擎，两条都已落地，设计脑在第一步定完文字层级就知道该走哪条。

| 图类型 | 特征 | 走哪条引擎 | 脚本 | key |
|--------|------|-----------|------|-----|
| **版式图（封面/信息图/卡片，文字要逐字精确）** | 文字是主角，标题、术语、副标题一个字都不能错 | **无 key 版式渲染引擎**（guizang 简约设计）：选 HTML 模板→填真实数据→Playwright 截图 | `scripts/render.py` | 不要 key，免费 |
| **插画 / 概念图 / 吉祥物示意图** | 文字少（≤30%）或纯视觉，靠画面叙事 | **API 插画引擎**（粘土 3D 卡通）：编译英文 prompt 送 gpt-image 类模型 | `scripts/gen.py` | 要 key（自己配 base_url + key，接任意 OpenAI 兼容 API） |

**怎么判断走哪条**：看设计脑第一步翻出来的「文字层级」这一维。

- 文字层级是 `title-only` 或 `text-rich`，也就是图上有要逐字精确的中文标题、术语、数据，**走无 key 版式渲染引擎**，出 guizang 简约设计风。HTML/CSS 把字焊死在版面上，截出来一个像素不差，免费，不烧额度。
- 文字层级是 `none`（纯视觉），或者要治愈插画、概念画面、吉祥物示意，**走 API 插画引擎**，出粘土 3D 卡通风。这种靠画面叙事不靠文字，gpt-image-2 最稳。

**为什么这么分**：扩散模型本质画像素不是排版，文字稍长就拼错、字距乱、中文尤其崩。要精确版式就别交给 API，交给浏览器渲染引擎。要有手工粘土质感的治愈画面就别用 HTML 硬拼，交给扩散模型。两边各干各擅长的。

**拿不准的中间情况**：想要一张「有大标题 + 粘土插画背景」的封面，先拆。标题精确性优先就走版式引擎（guizang 简约，干净留白配暖粉强调色已经够有调性）。背景画面叙事优先、标题能压进一行短中文（≤14 字），就走 API 引擎那条立体粘土标题字控字句。别想着一条引擎全包。

---

## 规格文件机制（先落 prompts/NN.md 再出图）

**规格即真相。每张图先落规格，再出图。改图只动规格，不绕过规格直接改 prompt。**

每张图在 `prompts/` 下落一个 `NN-主题.md`（NN 是两位序号），内容固定四块：

```
# 01-粘土小机器人章节图

## Brief（6 维）
载体：文章章节图
受众：AI 圈从业者 + 普通读者
主题：小机器人帮你把重复活儿交给脚本
情绪：治愈、轻松
文字层级：none
比例：16:9

## 设计决策（查矩阵填，带理由）
（抄决策矩阵对应行 + 偏离理由）

## 最终 Prompt
\`\`\`
（送进 API 的那段完整英文 prompt，脚本就读这一块）
\`\`\`

## Invariant 清单（改图时必须逐条锁死）
- 主色：暖粉 #F5A8B8 + 奶油白 #FBF3EC + 米白底 #F6ECE4
- 主角：粉白小机器人（暖粉耳机、胸口爱心、深棕黑屏幕脸、笑眼）
- 动作：机器人对着浮空积木堆比划，道具悬浮环绕
- 材质：哑光手工粘土、圆角无棱、柔光软影、次表面散射
```

**脚本只读「最终 Prompt」小标题下的第一个围栏代码块。** 这个小标题和代码块是硬约定，少了脚本报错。比例可以写进规格（`比例: 16:9`），脚本会自动捞。

**改图怎么改**：只动 invariant 里的单一变量（比如只换主色），其余 invariant 逐条复述进新 prompt，别整段重写，否则就漂了。改完落一个新序号的规格文件，老的留着对照。

---

## 怎么调 scripts/render.py（无 key 版式渲染引擎）

版式图（封面/信息图/卡片，文字要逐字精确）走这条。**零 API、免费、文字一个像素不差。** 机制学 guizang 的种子 HTML + Playwright 截图，签名就是 guizang 简约设计风（瑞士 / 编辑杂志质感），默认强调色换成黑哥暖粉，跟粘土签名同一套暖色家族。

三步走：**选模板→填真实数据→截图**。

**第一步：选模板**。`templates/` 下按载体挑：

| 模板 | 载体 | 尺寸（焊死在 CSS） |
|------|------|------|
| `templates/cover-clean.html` | 公众号 / 文章 / 小红书封面（单一焦点 + 大留白） | 默认竖板 1080×1440，可切 square 1080×1080、wide 2100×900 |
| `templates/infographic-clean.html` | 信息图 / 要点 / 对比 / 流程 / 数据清单（多级文字） | 默认竖板 1080×1440，四种数据形态切换 |

**第二步：填真实数据**。模板里所有 `{{变量}}` 手填成真内容，不靠模板引擎，直接改 HTML。每个模板顶部注释列了变量含义（`{{TITLE}}` `{{KICKER}}` `{{LEAD}}` 这些）和强调色切换（`data-accent="heige-blush"` 默认黑哥暖粉）。标题走黑哥文风：口语、短、断行用 `<br>`，标题太长砍字别缩字号。模板自带一组示例数据，浏览器直接打开能预览，正式渲染前把 `{{}}` 手填掉。

**第三步：截图**。模板里每张版式图包一个 `<section class="poster ...">`，render.py 按节点 bounding box 出图，节点多大就出多大像素，所见即所得。

```bash
# 截某个节点，指定输出（最常用）
python3 scripts/render.py templates/cover-clean.html --node "#cover" -o outimage/01-封面.png

# 不指定节点，抓页面里所有 section.poster 逐张出（信息图多卡片时）
python3 scripts/render.py templates/infographic-clean.html -o outimage/

# 默认 2x 高清，慢网或重渲染把 settle 调大
python3 scripts/render.py templates/cover-clean.html --node "#cover" --scale 2 --settle-ms 1200
```

不传 `-o` 默认落 `outimage/`，文件名按节点 id。`--full-page` 是兜底，模板没标 `.poster` 时强截整页。

**为什么文字不崩**：字是 HTML 文本，浏览器拿系统 PingFang SC 渲染，断网也不出豆腐块。米白底、暖粉强调色、发丝线网格、字号反差全是纯 CSS，不依赖任何 API 和外部图片。

**简约的命门是克制**：留白当主角，整张不超 3 色，一个强调色点题，别堆装饰形状。这是学 guizang 的硬规则，照着 `references/design-decision-matrix.md` 那条单轮 checklist 过一遍。

**首次用要装依赖**（脚本检测到没装会再提示一遍）：

```bash
pip install -r requirements.txt && playwright install chromium
```

**headless 已焊好的坑**：脚本默认带 `--use-angle=swiftshader`，否则 SVG / WebGL 滤镜在无头模式下会截成空白。截图前等 `document.fonts.ready` 再多停 800ms，避开截到 fallback 字体那一帧。这两个坑学的 guizang，已经焊进 render.py，调用方不用管。

---

## 怎么调 scripts/gen.py（API 插画引擎）

插画/概念图/吉祥物示意图走这条，出粘土 3D 卡通风。设计脑三步走完、规格文件落好，由脚本出图。

**接口通用，自己配 base_url + key + model**。脚本走 OpenAI 兼容的图像生成接口，三项配置方式都一样，优先级从高到低：命令行 > 环境变量 > 配置文件 > 默认值。

| 配置项 | 命令行 | 环境变量 | 默认值 |
|--------|--------|----------|--------|
| API 基址 | `--base-url` | `HEIGE_IMAGE_BASE_URL` | `https://api.openai.com/v1` |
| API Key | `--api-key` | `HEIGE_IMAGE_API_KEY` | 无（必填一项） |
| 模型 | `--model` | `HEIGE_IMAGE_MODEL` | `gpt-image-2` |

配置文件放 `~/.heige-image/config.json`，内容：

```json
{"base_url": "https://api.openai.com/v1", "api_key": "sk-xxx", "model": "gpt-image-2"}
```

官方 OpenAI、Azure、各家中转都能接，填自己那把就行。

> 国内想直接出图、不想自己折腾通道，可以用 gptx.cc 这个渠道，base_url 填 `https://api.gptx.cc/v1` 加上自己的 key 即可，作为推荐选项之一，不写死也不强制。

**主推：规格驱动出图**

```bash
python3 scripts/gen.py --spec prompts/01-粘土小机器人章节图.md -ar 16:9
```

不传 `-o` 默认落到 `outimage/01-粘土小机器人章节图.png`，比例不传就读规格里写的，规格也没写就 1:1。

**临时直给提示词（不落规格，不推荐常用）**

```bash
python3 scripts/gen.py --prompt "完整英文 prompt" -ar 16:9 -o outimage/test.png
```

**并发批量（一组配图）**

```bash
python3 scripts/gen.py --batch tasks.json --workers 2
```

`tasks.json` 是数组，每项 `{"prompt": "...", "output": "outimage/xx.png", "aspect_ratio": "16:9"}`。批量张数超 `--max-n`（默认 6）会被成本护栏直接拦。

**比例只有三个真实桶**：横→1536×1024、竖→1024×1536、方→1024×1024。写 `21:9` 跟写 `16:9` 出来一样大，比例是构图意图不是像素。章节图 / 16:9 总结图都走横桶 `-ar 16:9`，小红书插画走竖桶 `-ar 3:4`。

---

## 怎么调 scripts/edit.py（图生图 / 图片编辑引擎）

上传本地图片 + 文字编辑描述，模型按描述改图。配置约定和 gen.py 完全一致（CLI > `HEIGE_IMAGE_*` 环境变量 > `~/.heige-image/config.json` > 默认值），打的是 `/images/edits` 端点。

**单图编辑**：

```bash
python3 scripts/edit.py \
  --input /path/to/original.jpg \
  --prompt "把背景换成古镇青石板路，人物姿态和服装不变" \
  -ar 3:4 -o outimage/edited.png --retry 3
```

**多图输入**（`--input` 可重复传入，图1=基底图，图2 及之后=人脸/风格参考图，prompt 里按「图1/图2」指代各图角色）：

```bash
python3 scripts/edit.py \
  --input /path/to/scene.jpg \
  --input /path/to/face.jpg \
  --prompt "以图1为场景基底，把图2的人物自然融入画面中央，保持图1的光线和构图" \
  -ar 16:9 -o outimage/merged.png --retry 3
```

**并发批量**（`input` 字段接受字符串或数组）：

```bash
python3 scripts/edit.py --batch tasks.json --workers 2
```

```json
[
  {"input": "/path/a.jpg", "prompt": "换成雪景", "output": "outimage/a-snow.png"},
  {"input": ["/path/scene.jpg", "/path/face.jpg"], "prompt": "以图1为基底融入图2人物", "aspect_ratio": "16:9", "output": "outimage/merged.png"}
]
```

细节：单图走 multipart 的 `image` 字段，多图走多个 `image[]` 字段；输入支持 jpg/jpeg/png/webp/gif，单张 < 10MB；输出只接受完整 PNG，下载带有界流式读取和重定向，比例三桶规则和 gen.py 相同。

---

## 引用三个 references 的时机

| 文件 | 什么时候读 |
|------|-----------|
| `references/heige-visual-signature.md` | **第三步编译插画 prompt 时必读**。粘土签名的 color hex、材质词、3D 光感、吉祥物定义、prompt 骨架、控字两套、出图前后的一眼识别清单都在这。出图后拿第九节的验收清单逐条过。 |
| `references/design-decision-matrix.md` | **第二步设计决策时必读**。四大载体的引擎分流、六维默认、单轮 checklist、三条全局铁律、文章配图四子类都在这。 |
| `references/anti-patterns.md` | **出图前过 prompt、出图后过成品时读**。反 AI 体检清单两段 + 能力圈声明。拿不准这张是不是 AI 味，翻这个。 |

---

## 成本护栏

**只管 API 引擎（`gen.py`）。** 版式引擎（`render.py`）零 API、不烧额度，随便截，护栏不约束它。

API 引擎烧的是真额度，两道护栏卡死：

- **单次张数上限**：`scripts/gen.py --max-n` 默认 6，批量超了直接拦。要调高得明确知道在烧多少钱。一组配图压在 6 张内。
- **回炉最多 1 轮**：出图后过体检清单，中招就回炉重出，**最多回炉 1 轮**。1 轮还不行就降级处理（换主体重写 prompt，或老实承认这张不该用 API 做），别无限重试烧钱。

---

## 能力边界（诚实）

**现在能做（两条引擎都已落地）**：

- **版式图（无 key 引擎，`render.py`）**：公众号封面、信息图、卡片这类文字要逐字精确的图。HTML/CSS 焊死版面，Playwright 截图，免费、不烧额度、中文一个像素不差。出 guizang 简约设计风（米白底 + 暖粉强调色 + 大留白 + 发丝线网格），全纯 CSS。
- **插画图（API 引擎，`gen.py`）**：治愈插画、概念图、吉祥物示意图。配黑哥粘土 3D 卡通签名（暖粉加奶油白 + 手工粘土材质 + 粉白小机器人），设计脑三步全程在岗，规格文件可复现可改单变量。甜区是章节图和 16:9 总结图（横构图、少字、小机器人演出本章动作）。
- **图生图（API 引擎，`edit.py`）**：单图编辑（换背景、改元素）和多图合成（图1基底 + 图2人脸/风格参考），批量并发同 gen.py。2026-07-10 真实双图调用验证通过。

**各自需要什么**：

- 版式引擎要 Playwright + chromium（`pip install -r requirements.txt && playwright install chromium`），装一次到处用，不要 API key。
- API 引擎要一把能用的 key（自己配 base_url + key，接任意 OpenAI 兼容 API，国内可选 gptx.cc 这个渠道）。没 key 设计脑前三步照跑，能交付规格文件，就是出不了图。

**还是不做**：

- **API 引擎的图里文字别指望写对**，压在 30% 以内且默认可能拼错。要可读正确的文字走版式引擎那条。
- **API 引擎尺寸只有三种**，别信脚本里那十种比例，全映射到方/横/竖三个桶。要真正超宽幅/精确画幅 API 做不到，版式引擎可以在 CSS 里焊任意尺寸。
- **粘土质感的复杂画面别用版式引擎硬拼**，HTML 截图只到「简约版式 + 精确文字」这层，要手工粘土治愈插画交给 API 引擎。
- **不串别人画风**，只出黑哥这两套（插画粘土 3D、版式 guizang 简约），废弃旧版波普拼贴讽刺。

**一句话定位**：要逐字精确的版式图走无 key 的 `render.py`，出 guizang 简约设计、免费且文字不崩；要画面叙事的治愈插画概念图走要 key 的 `gen.py`，出粘土 3D 卡通。两条引擎各干各擅长的，同一套暖色域绑住，一眼认得出是黑哥。
